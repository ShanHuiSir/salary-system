# 每日工作总结与复盘 - 2026-07-15

## 今日总结
- 优化薪资系统中的薪酬数据录入与计算逻辑。
- 新增“总业绩 + 分项业绩缺一项自动补差”能力，减少人工计算。
- 将人数、成本、业绩变化后的环比/同比等指标改为基于原始数据自动重算。
- 将敏感薪资统计字段统一纳入固定公式计算，避免手工修改导致口径不一致。
- 补充面向薪资管理人员的操作说明，便于后续使用和培训。

## 关键改动
- 新增集中计算文件：`preview_app/extracted/salary-dashboard-deploy/src/utils/salaryCalculations.ts`。
- 数据新增、编辑、批量导入后统一执行薪资指标重算。
- 表单中系统计算字段增加“固定公式”标识，并禁用人工编辑。
- 月度总览支持总业绩与分项业绩之间的自动补差/汇总。

## 数据/功能验证
- 已运行 `npm run build`。
- 构建结果：通过。
- 当前本地访问地址：`http://127.0.0.1:5173/`。
- 默认登录账号：`admin`。
- 默认登录密码：`admin`。

## 问题与风险
- 如果历史月份缺少上月或去年同月数据，环比/同比会按现有数据计算，可能出现较大波动。
- 当前系统仍使用前端本地存储，正式用于薪资敏感数据前建议接入后端权限、审计和数据加密。
- Vite 构建提示部分资源包较大，后续如上线可考虑代码分包优化。

## 复盘
- 做得好的地方：将薪资计算逻辑集中管理，减少多个页面重复维护公式的风险。
- 可以改进的地方：后续可增加公式说明页、导入前数据校验报告、操作日志明细。
- 明日跟进：让薪资管理人员按真实数据试录一轮，重点检查自动补差、环比、同比和固定公式结果是否符合管理口径。
